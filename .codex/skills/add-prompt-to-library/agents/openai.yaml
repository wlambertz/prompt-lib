interface:
  display_name: "Add Prompt To Library"
  short_description: "Curate prompt ideas and scaffold high-value library entries"
  default_prompt: "Use $add-prompt-to-library to check this prompt idea against the library, propose the best category and filename, and add it only if it clears the curation bar."

policy:
  allow_implicit_invocation: true
