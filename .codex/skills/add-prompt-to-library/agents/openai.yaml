interface:
  display_name: "Add Prompt To Library"
  short_description: "Review prompt drafts for final library admission"
  default_prompt: "Use $add-prompt-to-library to review this prompt draft against the library, make the add or extend or reject decision, and assign the final category and filename."

policy:
  allow_implicit_invocation: false
