interface:
  display_name: "Prompt Librarian"
  short_description: "Find the best prompt by keywords or task fit"
  default_prompt: "Use $find-prompt-in-library to find the best prompt in this library from a few keywords or a short task description, and recommend authoring only if no good fit exists."

policy:
  allow_implicit_invocation: false
